// Unpacks a normalmap from Y and W coords. 
cVector3f UnpackNormalmapYW(in cVector4f avNormalValue)
{
	cVector3f vNormal = avNormalValue.yxx*2 - 1;
	vNormal.z = sqrt(1 - min(dot(vNormal.xy,vNormal.xy),1) );
	return vNormal;	
}
