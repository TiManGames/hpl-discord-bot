{{ScriptingStub}}

Have some helpful descriptions to add to this class? Edit this page and add your insight to the Wiki!

==Fields==

{| class="wikitable"
! Field Name !! Type !! Description
|-
| hours || <syntaxhighlight lang="c++" inline>int</syntaxhighlight> ||
|-
| minutes || <syntaxhighlight lang="c++" inline>int</syntaxhighlight> ||
|-
| month || <syntaxhighlight lang="c++" inline>int</syntaxhighlight> ||
|-
| month_day || <syntaxhighlight lang="c++" inline>int</syntaxhighlight> ||
|-
| seconds || <syntaxhighlight lang="c++" inline>int</syntaxhighlight> ||
|-
| week_day || <syntaxhighlight lang="c++" inline>int</syntaxhighlight> ||
|-
| year || <syntaxhighlight lang="c++" inline>int</syntaxhighlight> ||
|-
| year_day || <syntaxhighlight lang="c++" inline>int</syntaxhighlight> ||
|}

==Functions==

cDate has no public functions

==Remarks==

To get the current date, use the [[cSystem_GetDate]] function.

{{ReferencesSection}}

{{HPL3SOMAScriptingCategories}}
__FORCETOC__