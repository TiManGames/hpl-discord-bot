//-------------------------------------------------------

/////////////////////////////////////////
// DEEP SEA SUIT MONSTER
/////////////////////////////////////////

//-------------------------------------------------------

void DeepseaSuit_Wait(const tString&in asName, float afTime)
{
	cScript_SetGlobalArgFloat(0, afTime);
	cScript_RunGlobalFunc(asName, "cScrAgentDeepseaSuit", "_Global_Wait");
}

//-------------------------------------------------------

/////////////////////////////////////////
// SWIMBOTS
/////////////////////////////////////////

//-------------------------------------------------------

/**
 * Disables engine and other sounds coming from a swimbot. For non-engine sounds, behavior has to be implemented in the specific swimbot class.
 * 
 * @param asEntityName, name of swimbot.
 * @param abState, true = sounds will be disabled.
 **/
void Swimbot_SetSoundsDisabled(const tString &in asEntityName, bool abState)
{
	cScript_SetGlobalArgBool(0, abState);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentSwimBot", "_Global_SetSoundDisabled");
}

//-------------------------------------------------------

/////////////////////////////////////////
// SWIMBOT WORKER
/////////////////////////////////////////


void Worker_SetIdleMode_Scavenger(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_SetIdleMode_Scavenger");
}

void Worker_SetIdleMode_Wanderer(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_SetIdleMode_Wanderer");
}

void Worker_SetIdleMode_HeadBanger(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_SetIdleMode_HeadBanger");
}

void Worker_SetIdleMode_DoNothing(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_SetIdleMode_DoNothing");
}

void Worker_SetSpeedState(const tString &in asEntityName, int alState)
{
	cScript_SetGlobalArgInt(0,alState);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_SetSpeedState");
}

void Worker_SetMood_Avoid(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_SetMood_Avoid");
}

void Worker_SetMood_Attack(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_SetMood_Attack");
}

bool Worker_IsAttacking(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_IsAttacking");
	return cScript_GetGlobalReturnBool();
}

void Worker_SetOnAttackCallback(const tString &in asEntityName, const tString &in asCallback)
{
	cScript_SetGlobalArgString(0, asCallback);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentWorker", "_Global_SetOnAttackCallback");
}

//-------------------------------------------------------

/////////////////////////////////////////
// SWIMBOT GOYA
/////////////////////////////////////////

//-------------------------------------------------------

void Goya_Wander(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentGoya", "_Global_Wander");
}

//-------------------------------------------------------

/**
 * Sets the distance at which Goya will try to get away from the player.
 * 
 * @param asEntityName, Name of Goya robot
 * @param afFleeDist, distance to set.
 * @param asHomeEntity, entity to not stray too far from.
 * @param afMaxDistFromHome, max distance to stray from home entity.
 **/
void Goya_FleeFromPlayer(const tString &in asEntityName, float afFleeDist, const tString &in asHomeEntity, float afMaxDistFromHome)
{
	cScript_SetGlobalArgFloat(0, afFleeDist);
	cScript_SetGlobalArgString(1, asHomeEntity);
	cScript_SetGlobalArgFloat(2, afMaxDistFromHome);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentGoya", "_Global_FleeFromPlayer");
}

//-------------------------------------------------------

/**
 * Slows down the flee speed of Goya
 * 
 * @param asEntityName, Name of Goya robot
 **/
void Goya_SlowDownFleeSpeed(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentGoya", "_Global_SlowDownFleeSpeed");
}

//-------------------------------------------------------

/**
 * Reduces the health of Goya.
 * 
 * @param asEntityName, Name of Goya robot
 * @param afAmount, amount to reduce health by. Max health is 1.0
 **/
void Goya_ReduceHealth(const tString &in asEntityName, float afAmount)
{
	cScript_SetGlobalArgFloat(0, afAmount);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentGoya", "_Global_ReduceHealth");
}

//-------------------------------------------------------

/**
 * Crashes Goya
 * 
 * @param asEntityName, Name of Goya robot
 * @param asCrashTarget, entity to crash towards.
 * @param asCallback, callback when the robot has reached its target. Syntax: void Func(const tString& in asEntityName, bool abReachedEnd)
 **/
void Goya_Crash(const tString &in asEntityName, const tString &in asCrashTarget, const tString &in asCallback)
{
	cScript_SetGlobalArgString(0, asCrashTarget);
	cScript_SetGlobalArgString(1, asCallback);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentGoya", "_Global_Crash");
}

//-------------------------------------------------------

/**
 * Checks if Goya is moving
 * 
 * @param asEntityName, Name of Goya robot
 * @return bool, true if Goya is moving or has stopped temporarily.
 **/
bool Goya_IsMoving(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentGoya", "_Global_IsMoving");
	return cScript_GetGlobalReturnBool();
}

//-------------------------------------------------------

/////////////////////////////////////////
// SWIMBOT KATE COPY
/////////////////////////////////////////

//-------------------------------------------------------

/**
 * Makes the kate copy refuel at this point.
 * 
 * @param asEntityName, Name of Kate Copy robot
 * @param asRefuelAtEntity, The entity to refuel at. The positive z-axis must point where the robot will come to rest.
 * @param asRefuelDoneCallback, Called when refueling is done. Syntax: void Func(const tString&in asEntity);
 **/
void KateCopy_RefuelAt(const tString &in asEntityName, const tString &in asRefuelAtEntity, const tString &in asRefuelDoneCallback)
{
	cScript_SetGlobalArgString(0, asRefuelAtEntity);
	cScript_SetGlobalArgString(1, asRefuelDoneCallback);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentKateCopy", "_Global_RefuelAt");
}

//-------------------------------------------------------
/**
 * Callback Syntax: void Func(const tString& in asEntityName, bool abReachedEnd)
 *  movement speed. 0 = slower, 1 = faster.
 **/
void KateCopy_MoveTo(const tString &in asEntityName,const tString &in asNodeName,const tString &in asCallbackFunc, int alState)
{
	cScript_SetGlobalArgString(0, asNodeName);
	cScript_SetGlobalArgString(1, asCallbackFunc);
	cScript_SetGlobalArgInt(2, alState);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentKateCopy", "_Global_MoveTo");
}

//-------------------------------------------------------

/**
 * Sets the movement speed. 0 = slower, 1 = faster.
 **/
void KateCopy_SetSpeedState(const tString &in asEntityName, int alState)
{
	cScript_SetGlobalArgInt(0, alState);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentKateCopy", "_Global_SetSpeedState");
}

//-------------------------------------------------------

/**
 * Makes the kate copy investigate the current areas. Needs pathnodes to find points of interest.
 **/
void KateCopy_Investigate(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentKateCopy", "_Global_Investigate");
}

//-------------------------------------------------------

/////////////////////////////////////////
// CONSTRUCT WORKER
/////////////////////////////////////////

//-------------------------------------------------------

void ConstructWorker_SetMoodDocile(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SetMoodDocile");
}

void ConstructWorker_SetMoodAgressive(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SetMoodAgressive");
}

void ConstructWorker_SetMoodCautious(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SetMoodCautious");
}

//-------------------------------------------------------

void ConstructWorker_SetIdleMode_HeadPound(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SetIdleMode_HeadPound");
}

void ConstructWorker_SetIdleMode_Normal(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SetIdleMode_Normal");
}

void ConstructWorker_SetIdleMode_Stalk(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SetIdleMode_Stalk");
}

void ConstructWorker_SetIdleMode_Scavenge(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SetIdleMode_Scavenge");
}

void ConstructWorker_SetSpeedState_Dash(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SpeedState_Dash");
}

void ConstructWorker_SetSpeedState_Wander(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SpeedState_Wander");
}

void ConstructWorker_SetMoveSoundsActive(const tString &in asEntityName, bool abActive)
{
	cScript_SetGlobalArgBool(0,abActive);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentConstructWorker", "_Global_SetMoveSoundsActive");
}

//-------------------------------------------------------

/////////////////////////////////////////
// SWIMBOT CUTE BOT
/////////////////////////////////////////

//-------------------------------------------------------

void CuteBot_SetSwarmPoint(const tString &in asEntityName, const tString &in asHomeEntity)
{
	cScript_SetGlobalArgString(0, asHomeEntity);
	cScript_RunGlobalFunc(asEntityName, "cScrAgentCuteBot", "_Global_SetSwarmPoint");
}

//-------------------------------------------------------

/////////////////////////////////////////
// SWARM
/////////////////////////////////////////

//-------------------------------------------------------

void Swarm_SetPlayerIsInBuilding(const tString&in asName, bool abX)
{
	cScript_SetGlobalArgBool(0, abX);
	cScript_RunGlobalFunc(asName, "cScrAgentSwarm", "_Global_SetPlayerIsInBuilding");
}

//-------------------------------------------------------

void Swarm_SetBuildUpSpeedMul(const tString&in asName, float afMul)
{
	cScript_SetGlobalArgFloat(0, afMul);
	cScript_RunGlobalFunc(asName, "cScrAgentSwarm", "_Global_SetBuildUpSpeedMul");
}

//-------------------------------------------------------

void Swarm_StartDeactivation(const tString&in asName)
{
	cScript_RunGlobalFunc(asName, "cScrAgentSwarm", "_Global_StartDeactivation");
}

//-------------------------------------------------------

void Swarm_RetreatTowards(const tString& in asName, const tString& in asEntityName, float afTime)
{
	cScript_SetGlobalArgString(0, asEntityName);
	cScript_SetGlobalArgFloat(1,afTime);
	cScript_RunGlobalFunc(asName, "cScrAgentSwarm", "_Global_RetreatTowards");
}

//-------------------------------------------------------

/////////////////////////////////////////
// CONSTRUCT WORKER
/////////////////////////////////////////

//-------------------------------------------------------

void ConstructWorker_StartFeed(	const tString &in asConstruct, const tString &in asFoodEntity, 
								const tString &in asStopAtPathNode,
								float afFeedTime, array<tString> &in avAttachAreas)
{
	/////////////////////////////
	// Get Food Entity
	iLuxEntity @pFood = cLux_GetCurrentMap().GetEntityByName(asFoodEntity);
	if(pFood is null)
	{
		Error("Could not find food entity '"+asFoodEntity+"' for '"+asConstruct+"' feeding");
		return;
	}
	
	/////////////////////////////
	// Get attach areas
	array<cVector3f> vAttachPos;
	vAttachPos.resize(avAttachAreas.size());
	for(uint i=0; i<avAttachAreas.size(); ++i)
	{
		iLuxEntity @pArea = cLux_GetCurrentMap().GetEntityByName(avAttachAreas[i]);
		if(pArea is null)
		{
			Error("Could not find attach area '"+avAttachAreas[i]+"' for '"+asConstruct+"' feeding");
			return;
		}
		vAttachPos[i] = pArea.GetPosition();
	}
	
	/////////////////////////////
	// Call function
	cScript_SetGlobalArgID(0, pFood.GetID());
	cScript_SetGlobalArgString(1, asStopAtPathNode);
	cScript_SetGlobalArgFloat(2, afFeedTime);
	cScript_SetGlobalArgBool(3, true); //Setting that this IS a scripted feed.
	
	cScript_SetGlobalArgInt(4, vAttachPos.size());
	for(uint i=0; i<vAttachPos.size(); ++i)
	{
		cScript_SetGlobalArgVector3f(5+i, vAttachPos[i]);
	}
	
	cScript_RunGlobalFunc(asConstruct, "", "_Global_StartFeed");
}

//-------------------------------------------------------

/////////////////////////////////////////
// INFECTED ROBOT
/////////////////////////////////////////

//-------------------------------------------------------

/**
 * Forces the specified infected robot in to an idle state
 * @param asEntityName Name of the entity
 */
void InfectedRobot_ForceIdleState(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentInfectedRobot", "_Global_ForceIdleState");
}

//-------------------------------------------------------

/**
 * Forces the specified infected robot in to a disabled state
 * @param asEntityName Name of the entity
 */
void InfectedRobot_ForceDisabledState(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentInfectedRobot", "_Global_ForceDisabledState");
}

//-------------------------------------------------------

/**
 * Forces the specified infected robot in to an idle state
 * @param asEntityName Name of the entity
 */
void InfectedRobot_ForceHuntState(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAgentInfectedRobot", "_Global_ForceHuntState");
}

//-------------------------------------------------------

void InfectedRobot_SetSlowHuntSpeed(const tString &in asEntityName, bool abX)
{
	cScript_SetGlobalArgBool(0, abX);
	cScript_RunGlobalFunc(asEntityName,"cScrAgentInfectedRobot","_Global_SetSlowHuntSpeed");
}

//-------------------------------------------------------


/////////////////////////////////////////
// ROOMBA
/////////////////////////////////////////

//-------------------------------------------------------

/**
 * Moves the Roomba to a position
 * 
 * @param asName, name of the Roomba. Wildcard supported.
 **/
void Roomba_StartMoving(const tString &in asName)
{
	if(cScript_RunGlobalFunc(asName, "cScrRoomba", "_Global_StartMoving")==false)
	{
		Error("Could not find prop with name '"+asName+"' for Roomba_StartMoving");
		return;
	}
}

//-------------------------------------------------------

/**
 * Stops the Roomba
 * 
 * @param asName, name of the Roomba. Wildcard supported.
 **/
void Roomba_StopMoving(const tString &in asName)
{
	if(cScript_RunGlobalFunc(asName, "cScrRoomba", "_Global_StopMoving")==false)
	{
		Error("Could not find prop with name '"+asName+"' for Roomba_StopMoving");
		return;
	}
}

//-------------------------------------------------------

/////////////////////////////////////////
// VIPERFISH
/////////////////////////////////////////

//-------------------------------------------------------

void Viperfish_SetIdle(const tString &in asEntityName)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for Viperfish_SetIdle");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrViperfish", "_Global_SetIdle");
	}
}

//-------------------------------------------------------

void Viperfish_SetIdleCallback(const tString &in asEntityName, const tString& in asCallback)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for Viperfish_SetIdleCallback");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_SetGlobalArgString(0, asCallback);
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrViperfish", "_Global_SetIdleCallback");
	}
}
//-------------------------------------------------------

void Viperfish_SetAttackCallback(const tString &in asEntityName, const tString& in asCallback)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for Viperfish_SetAttackCallback");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_SetGlobalArgString(0, asCallback);
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrViperfish", "_Global_SetAttackCallback");
	}
}

//-------------------------------------------------------

void Viperfish_SetCircling(const tString &in asEntityName, bool abShouldReposition=false)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for Viperfish_SetCircle");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_SetGlobalArgBool(0, abShouldReposition);
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrViperfish", "_Global_SetCircling");
	}
}

//-------------------------------------------------------

void Viperfish_SetPatrol(const tString &in asEntityName)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for Viperfish_SetPatrol");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrViperfish", "_Global_SetPatrol");
	}
}

//-------------------------------------------------------

void Viperfish_SetConverge(const tString &in asEntityName, const tString& in asTargetEntity)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for Viperfish_SetConverge");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_SetGlobalArgString(0, asTargetEntity);
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrViperfish", "_Global_SetConverge");
	}
}

//-------------------------------------------------------

void Viperfish_SetGoToPos(const tString &in asEntityName, const cVector3f &in vPos)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for Viperfish_SetConverge");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_SetGlobalArgVector3f(0, vPos);
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrViperfish", "_Global_SetGoToPos");
	}
}

//-------------------------------------------------------

void Viperfish_SetSlowAttack(const tString &in asEntityName, bool abSlow)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for Viperfish_SetSlowAttack");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_SetGlobalArgBool(0, abSlow);
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrViperfish", "_Global_SetSlowAttack");
	}
}

//-------------------------------------------------------

/////////////////////////////////////////
// ANGLERFISH
/////////////////////////////////////////

//-------------------------------------------------------

void Anglerfish_SetIdle(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAnglerfish", "_Global_SetIdle");
}

//-------------------------------------------------------

void Anglerfish_SetIdleCallback(const tString &in asEntityName, const tString& in asCallback)
{
	cScript_SetGlobalArgString(0, asCallback);
	cScript_RunGlobalFunc(asEntityName, "cScrAnglerfish", "_Global_SetIdleCallback");
}
//-------------------------------------------------------

void Anglerfish_SetAttackCallback(const tString &in asEntityName, const tString& in asCallback)
{
	cScript_SetGlobalArgString(0, asCallback);
	cScript_RunGlobalFunc(asEntityName, "cScrAnglerfish", "_Global_SetAttackCallback");
}

//-------------------------------------------------------

void Anglerfish_SetGoToPos(const tString &in asEntityName, const cVector3f &in vPos)
{
	cScript_SetGlobalArgVector3f(0, vPos);
	cScript_RunGlobalFunc(asEntityName, "cScrAnglerfish", "_Global_SetGoToPos");
}

//-------------------------------------------------------

void Anglerfish_SetActivateCallback(const tString &in asEntityName, const tString& in asCallback)
{
	cScript_SetGlobalArgString(0, asCallback);
	cScript_RunGlobalFunc(asEntityName, "cScrAnglerfish", "_Global_SetActivateCallback");
}

//-------------------------------------------------------

void Anglerfish_ActivateAtPosAwayFromPlayer(const tString &in asEntityName, float afDist)
{
	cScript_SetGlobalArgFloat(0, afDist);
	cScript_RunGlobalFunc(asEntityName, "cScrAnglerfish", "_Global_ActivateAtPosAwayFromPlayer");
}

//-------------------------------------------------------

void Anglerfish_AttackPlayer(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAnglerfish", "_Global_AttackPlayer");
}

//-------------------------------------------------------

void Anglerfish_ForceLeave(const tString &in asEntityName)
{
	cScript_RunGlobalFunc(asEntityName, "cScrAnglerfish", "_Global_ForceLeave");
}

//-------------------------------------------------------

/////////////////////////////////////////
// MEGAMOUTH
/////////////////////////////////////////

//-------------------------------------------------------

void MegaMouth_SetAttackCallback(const tString &in asEntityName, const tString& in asCallback)
{
	array<iLuxEntity@> vEntities;
	cLux_GetCurrentMap().GetEntityArray(asEntityName, eLuxEntityType_Agent, "", vEntities);
	if(vEntities.length() == 0)
	{
		Error("Could not find any entities with name '"+asEntityName+"' for MegaMouth_SetAttackCallback");
		return;
	}
	
	for (uint i=0;i<vEntities.size();++i)
	{
		cScript_SetGlobalArgString(0, asCallback);
		cScript_RunGlobalFunc(vEntities[i].GetName(), "cScrMegaMouth", "_Global_SetAttackCallback");
	}
}

//-------------------------------------------------------

/////////////////////////////////////////
// REMADE
/////////////////////////////////////////

//-------------------------------------------------------

void Remade_ForceHuntPause(const tString &in asEntityName, bool abForcePause)
{
	cScript_SetGlobalArgBool(0, abForcePause);
	cScript_RunGlobalFunc("agent_robothead_tunnelchase","cScrAgentRemade","_Global_ForceHuntPause");
}

//-------------------------------------------------------

void Remade_ForceSlowHunt(const tString &in asEntityName, bool abForceSlowHunt)
{
	cScript_SetGlobalArgBool(0, abForceSlowHunt);
	cScript_RunGlobalFunc("agent_robothead_tunnelchase","cScrAgentRemade","_Global_ForceSlowHunt");
}

//-------------------------------------------------------