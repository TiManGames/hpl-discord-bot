#include "PlayerState_UsingThrowable.hps"

//------------------------------------------------------------

class cScrPlayerState_UsingRabbitDoll : cScrPlayerState_UsingThrowable
{
	//------------------------------------------------------------
		
	/////////////////////////////////////////
	// INIT
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	void Init() override
	{
		cScrPlayerState_UsingThrowable::Init();
		
		mfThrowableMinPitch = cMath_ToRad(-55.f);
		mfThrowableMaxPitch = cMath_ToRad(30.f);
		mfThrowablePitchFadeTime = 0.25f;
		mfMaxThrowDelay = 1.f;

		msFocusIntroAnim_R = "tool_rabbit_focus_start";
		msFocusLoopAnim_R = "tool_rabbit_focus_idle";
		msFocusOutroAnim_R = "tool_rabbit_focus_end";
		msThrowAnim_R = "tool_rabbit_throw";
		msThrowAnim_Head = "tool_grenade_throw_head";
		
		msFocusIntroAnim_Item = "focus_start";
		msFocusLoopAnim_Item = "focus_idle";
		msFocusOutroAnim_Item = "focus_end";
		msThrowAnim_Item = "throw";
		
		msThrowEnt = "doll_rabbit_pickup.ent";
	}
	
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// GENERAL
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// ACTIONS
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// HELPERS
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	void ThrowProp(const tString &in asThrowEnt) override
	{
		cScrPlayerState_UsingThrowable::ThrowProp(asThrowEnt);
		iLuxEntity@ pEnt = cLux_GetCurrentMap().GetLatestEntity();
		if (pEnt is null)
			return;
		
		cLuxProp@ pProp = cLux_ToProp(pEnt);
		if (pProp is null)
			return;
			
		pProp.SetAllowMapTransfer(true);
	}
	
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// DEBUG
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// PROPERTIES
	/////////////////////////////////////////
	
	//------------------------------------------------------------
		
	//------------------------------------------------------------
}