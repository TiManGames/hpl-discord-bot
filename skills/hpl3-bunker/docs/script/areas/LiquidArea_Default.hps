#include "interfaces/LiquidArea_Interface.hps"

#include "helpers/helper_player.hps"
#include "helpers/helper_map.hps"
#include "helpers/helper_effects.hps"

class cScrLiquidAreaDefault : iScrLiquidArea, iScrLiquidArea_Interface
{
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// INIT
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	void Init()
	{
		
	}
	
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// LOADING
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	void SetupAfterLoad(cWorld @apWorld, cResourceVarsObject @apVars)
	{
		mbEffectOnExit = apVars.GetVarBool("EffectOnExit",true);
		msCameraEnterEffectPreset = apVars.GetVarString("CameraEnterEffectPreset", "");
		msCameraExitEffectPreset = apVars.GetVarString("CameraExitEffectPreset", "");
		mUnderwaterLightColor = apVars.GetVarColor("UnderwaterLightColor", cColor(0.f));

		Material_Preload("water_splash_screen.mat");
		Material_Preload("water_drip_screen.mat");
	}
	
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// GENERAL
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	void OnVariableUpdate(float afTimeStep)
	{
	}
	
	//------------------------------------------------------------
	
	void OnPostUpdate(float afTimeStep)
	{
	}
	
	//------------------------------------------------------------
	
	void ResetProperties()
	{
	}
	
	//------------------------------------------------------------
	
	void BeforeEntityDestruction()
	{
	}
		
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// INTERACTION
	/////////////////////////////////////////
	
	//------------------------------------------------------------
		
	bool CanInteract(int alType, iPhysicsBody@ apBody)
	{
		return false;
	}
	
	//------------------------------------------------------------
		
	bool OnInteract(int alType, iPhysicsBody@ apBody, const cVector3f &in avFocusPos, const tString&in asData)
	{
		return false;
	}
		
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// EXIT AND ENTER
	/////////////////////////////////////////
	
	//-----------------------------------------------------------------------

	void OnBodyEnterLiquid(iPhysicsBody @apBody)
	{
		//mBaseObj.GetMap().BroadcastSoundHeardEvent("WaterSplash", apBody.GetWorldPosition(), 20.f, 3);
		iLuxEntity@ pEnt = cLux_GetBodyEntity(apBody);
		if (pEnt !is null)
		{
			if (pEnt.GetClassName()=="cScrPropFlare")
			{
				cScript_RunGlobalFunc(pEnt.GetName(), "cScrPropFlare", "_Global_ForceSpent");
			}
		}
	}
	
	//-----------------------------------------------------------------------
	
	void OnBodyExitLiquid(iPhysicsBody @apBody)
	{
	}
	
	//-----------------------------------------------------------------------
	
	void OnCameraEnter()
	{
		if(msCameraEnterEffectPreset != "")
		{
			Map_Preset_Fade(msCameraEnterEffectPreset, 0.0);
		}
	}
	
	//-----------------------------------------------------------------------

	void OnCameraExit()
	{
		if(msCameraExitEffectPreset != "")
		{
			Map_Preset_Fade(msCameraExitEffectPreset, 0.0);
		}
		
		if(mbEffectOnExit && cLux_GetUnderwaterEffectsActive() == false)
		{
			Effect_Screen_Start("water_splash_screen.mat", cVector2f(0.5, 0.5), cVector2f(1.8, 1.8), 1, 0.4f, 0.2f, 0.6f); 
			Effect_Screen_Start("water_drip_screen.mat", cVector2f(0.5, 0.5), cVector2f(1.8, 1.8), 1, 1, 2, 1);
		}
	}

	//-----------------------------------------------------------------------
	
	/////////////////////////////////////////
	// ACTIONS
	/////////////////////////////////////////
	
	//-----------------------------------------------------------------------
	
	void OnSetActive(bool abX)
	{
		if(abX==false)
		{
			//Need to do this as it might miss some edge case.
			cLux_GetPlayer().RemoveUsedLiquidArea(GetBase());
			//cLux_AddDebugMessage("RemoveUsedLiquidArea in on set active");
		}
	}
	
	//-----------------------------------------------------------------------
	
	
	void OnConnectionStateChange(iLuxEntity@ apEntity,int alState)
	{
		if(alState==0) return;
	}

	//-----------------------------------------------------------------------
	
	/////////////////////////////////////////
	// GLOBAL
	/////////////////////////////////////////
	
	//-----------------------------------------------------------------------
	
	void _Global_SetEffectOnExit()
	{
		mbEffectOnExit = cScript_GetGlobalArgBool(0);
	}
	
	//-----------------------------------------------------------------------
	
	void _Global_GetUnderwaterLightColor()
	{
		cScript_SetGlobalReturnColor(mUnderwaterLightColor);
	}
	
	//-----------------------------------------------------------------------
	
	/////////////////////////////////////////
	// DEBUG
	/////////////////////////////////////////
	
	//------------------------------------------------------------
		
	float DrawDebugOutput(cGuiSet @apSet,iFontData @apFont,float afStartY)
	{
		return afStartY;
	}
	
	//------------------------------------------------------------
	
	/////////////////////////////////////////
	// PROPERTIES
	/////////////////////////////////////////
	
	//------------------------------------------------------------
	
	bool mbPlayerIsInside;
	bool mbEffectOnExit;
	tString msCameraEnterEffectPreset;
	tString msCameraExitEffectPreset;
	[nosave] cColor mUnderwaterLightColor;
	
	//------------------------------------------------------------
		
}	